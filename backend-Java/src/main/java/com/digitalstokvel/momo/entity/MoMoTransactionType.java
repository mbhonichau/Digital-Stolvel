package com.digitalstokvel.momo.entity;

public enum MoMoTransactionType {
    COLLECTION,     // RequestToPay (collecting contributions from member)
    DISBURSEMENT    // Transfer (payout disbursement to member)
}
