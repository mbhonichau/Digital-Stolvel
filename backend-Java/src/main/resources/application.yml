server:
  port: ${PORT:8080}

spring:
  application:
    name: digital-stokvel-backend

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  datasource:
    url: ${DB_URL:jdbc:h2:mem:stokveldb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH}
    driver-class-name: ${DB_DRIVER:org.h2.Driver}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      pool-name: StokvelHikariCP

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: true

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: true

  jackson:
    serialization:
      write-dates-as-timestamps: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.digitalstokvel: DEBUG
    org.flywaydb: INFO
    org.hibernate.SQL: INFO

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3001}

momo:
  base-url: ${MOMO_BASE_URL:https://proxy.momoapi.mtn.com}
  target-environment: ${MOMO_TARGET_ENV:mtnsouthafrica}
  currency: ${MOMO_CURRENCY:ZAR}
  mock-mode: ${MOMO_MOCK_MODE:true}
  poll-interval-ms: ${MOMO_POLL_INTERVAL_MS:5000}
  collection:
    subscription-key: ${MOMO_COLLECTIONS_SUBSCRIPTION_KEY:}
    api-user: ${MOMO_API_USER:}
    api-key: ${MOMO_API_KEY:}
  disbursement:
    subscription-key: ${MOMO_DISBURSEMENT_SUBSCRIPTION_KEY:}
    api-user: ${MOMO_API_USER:}
    api-key: ${MOMO_API_KEY:}

---
# PostgreSQL Production / Container profile
spring:
  config:
    activate:
      on-profile: postgres
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/stokveldb}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
