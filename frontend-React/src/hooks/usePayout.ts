import { useTriggerPayout, usePayoutStatus } from './usePayouts';

/**
 * Hook for managing and triggering payouts.
 * Standardized alias for useTriggerPayout.
 */
export const usePayout = useTriggerPayout;
export { useTriggerPayout, usePayoutStatus };

export default usePayout;
